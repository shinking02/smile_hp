import React from "react";
import ReactDOM from "react-dom";

const app = document.getElementById("app");
ReactDOM.render(
    <React.StrictMode>
        <h1>Hello World</h1>
    </React.StrictMode>,
    app
)